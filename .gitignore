node_modules/
dist/
*.log
.DS_Store

# Solaria art is licensed for use but not for redistribution, so it is dropped
# in locally instead of committed. See "Solaria art" in the README.
public/assets/solaria/
